const RewardListData = require("./../models/rewardListModel");

exports.UpdateClaimStatus = async (req, res) => {
  try {
    const { userId, taskId } = req.body;
    if (userId && taskId) {
    }
  } catch (err) {}
};
